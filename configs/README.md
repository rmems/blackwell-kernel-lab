# configs/

Engine and agent configuration snippets for this host.

- Do **not** commit API keys or private model paths with secrets.
- Prefer env vars for absolute model directories.
- Training configs belong in **`agoge-forger`**.
